import React from 'react';

const Invoice = () => {
    return (
        <div>
            Invoice Page, Where you can create and manage invoices, view payment history, and generate reports. This page allows you to keep track of your sales and customer transactions efficiently.
        </div>
    );
};

export default Invoice;